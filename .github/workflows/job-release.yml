name: Job - Release

on:
  workflow_call:

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v8
        with:
          path: ./artifacts
      - name: Create release with GitHub CLI
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create ${{ github.ref_name }} --generate-notes \
            ./artifacts/cyyc-linux-x86_64-ubuntu22/cyyc-linux-x86_64-ubuntu22 \
            ./artifacts/cyyc-linux-x86_64-ubuntu24/cyyc-linux-x86_64-ubuntu24 \
            ./artifacts/cyyc-linux-aarch64/cyyc-linux-aarch64 \
            ./artifacts/cyyc-macos-aarch64/cyyc-macos-aarch64
